# KiloCode Workflow Rules

Этот файл определяет законы взаимодействия агентов.
Нарушение этих правил приведет к сбою автоматизации.

## 🧠 INTELLIGENCE PROTOCOL (MANDATORY)
Агенты ОБЯЗАНЫ использовать инструменты KiloCode (`codebase_search`, MCP) вместо слепого чтения.
* **Codebase Index:** Используй `codebase_search` для понимания архитектуры и поиска зависимостей.
* **MCP Context:** Запрашивай документацию библиотек через MCP сервер `context7`.
* **Правило:** "Сначала разведка через Индекс — потом бой".

## 🧪 TDD PROTOCOL (MANDATORY)
Весь функциональный код пишется по циклу **RED -> GREEN -> REFACTOR**.
1. **RED:** Создай тест, который падает.
2. **GREEN:** Напиши минимальный код для прохождения теста.
3. **REFACTOR:** Улучши код (чистота, стандарты), не ломая тесты.
*Без падающего теста начинать реализацию запрещено.*

## 🐍 VIRTUAL ENVIRONMENT PROTOCOL
**ЗАПРЕЩЕНО** запускать код в глобальном окружении.
* Всегда используй `.venv`.
* Команда запуска: `call .venv\Scripts\activate && python ...` (или аналог).

## 🛡 ATOMIC TASK PROTOCOL (MANDATORY)
Мы работаем по принципу **"Single Piece Flow"**. Файл `current_task.md` — единственный источник истины.

### 1. Формат Тикетов (Task IDs)
Каждая задача ОБЯЗАНА иметь уникальный ID:
- **REF-XXX**: Рефакторинг (структурные изменения).
- **FEAT-XXX**: Новая функциональность.
- **FIX-XXX**: Исправление багов.
- **CHORE-XXX**: Конфиги, тесты.

### 2. Структура `current_task.md`
Файл состоит СТРОГО из двух секций:

#### 🟢 ACTIVE TASK
**Ровно ОДНА** задача.
- **Header:** `[ID] Title`
- **Context:** Зачем это нужно.
- **TDD Strategy:** Указание конкретного файла теста (напр. `tests/test_undo.py`).
- **Tools Strategy:** Что искать в Индексе?
- **Action Plan:** Детальные шаги (Red -> Green -> Refactor).

``

#### 🟡 BACKLOG
Список остальных задач.
Формат: `- [ ] **[ID]** Title`

## 👮 РОЛЕВАЯ МАТРИЦА (RBAC)

### 🧠 ARCHITECT (Planning)
* **Start:** Начинай с `codebase_search` для анализа.
* **Action:** Декомпозируй задачу. Присвой ID. Определи имя теста.
* **Limit:** Не пиши код реализации.

### 👨‍💻 CODER (Implementation)
* **Start:** Impact Analysis через Индекс.
* **Action:** Выполни TDD цикл (Red-Green-Refactor) в `.venv`.
* **Limit:** Не смотри в Backlog. Не пиши код без теста.

### ✅ QA (Acceptance)
* **Start:** Проверь наличие теста и стандарты кода.
* **Action:** Запусти тесты в `.venv`.