# Бриф проекта: Budget Bot

## 1. Общее описание
**Budget Bot** — это Telegram-бот для автоматизированного ведения личного бюджета. Система позволяет пользователям фиксировать расходы и доходы с минимальными усилиями, используя технологии распознавания чеков (QR-коды) и обработки естественного языка ("умный ввод"). Все данные надежно сохраняются локально и синхронизируются с Google Таблицами для удобной отчетности.

## 2. Основные цели
* **Минимизация рутины:** Сокращение времени на внесение транзакций за счет автоматизации (сканирование чеков, парсинг текста).
* **Точность данных:** Исключение ошибок ручного ввода и дублирования.
* **Надежность:** Обеспечение сохранности данных даже при отсутствии связи (Local-First архитектура).
* **Аналитика:** Предоставление структурированных данных в Google Sheets для построения графиков и отчетов.

## 3. Ключевой функционал
### 📸 Работа с чеками
* Распознавание QR-кодов на чеках (через API Proverkacheka.com).
* Автоматическое извлечение суммы, даты, продавца и списка товаров.
* Обучение системы на новых чеках (связывание продавцов с категориями).

### 🧠 Умная категоризация
* **ML-классификатор:** Определение категории транзакции на основе истории и ключевых слов.
* **Лемматизация:** Использование `pymorphy3` для понимания различных форм слов (например, "такси", "на такси", "таксисту").
* **Словари:** Поддержка биграмм и весовых коэффициентов уверенности для ключевых слов.

### 💬 Интерфейс и Ввод
* **Smart Input:** Распознавание текстовых команд в свободной форме (например, *"300 кофе"* или *"Такси 500"*).
* **Черновики:** Возможность редактировать транзакцию (категорию, сумму, комментарий) перед финальным сохранением.
* **История:** Просмотр последних транзакций с пагинацией прямо в чате.

### 🔄 Синхронизация и Хранение
* **Local-First:** Первичная запись в локальную БД (`SQLite`).
* **Sync Worker:** Фоновый процесс для надежной отправки данных в Google Sheets.
* **Кэширование:** Локальное хранение справочников категорий для ускорения работы.

## 4. Технологический стек
* **Язык:** Python 3.11+
* **Фреймворк:** Aiogram 3.x (асинхронный)
* **База данных:** SQLite (`aiosqlite`)
* **Google Sheets:** `gspread` + `gspread-asyncio`
* **Валидация данных:** Pydantic v2
* **NLP/ML:** `pymorphy3`, кастомный классификатор на основе TF-IDF и частотного анализа.
* **HTTP:** `aiohttp`

## 5. Архитектура проекта
Проект строится на модульной архитектуре с использованием паттернов:
* **Repository Pattern:** Для абстракции работы с БД.
* **Service Layer:** Выделенный `TransactionService` для бизнес-логики.
* **FSM (Finite State Machine):** Управление состояниями диалога с пользователем.
* **Kilo Code Workflow:** Разработка ведется системой автономных агентов (Orchestrator, Architect, Code, QA) с строгим соблюдением TDD и протоколов безопасности.