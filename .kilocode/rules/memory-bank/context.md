# Контекст: Проект Budget Bot

## Текущий фокус
Проект находится в фазе **глубокого архитектурного рефакторинга**. 
Мы уходим от "скриптового" стиля (глобальные переменные, монолитные файлы) к модульной Enterprise-архитектуре.
**Главный приоритет:** Устранение сильной связности кода и глобального состояния перед добавлением любой новой функциональности.

## Последние изменения
- ✅ Исправлена критическая уязвимость SQL-инъекции.
- ✅ Исправлена логика авторизации (проверка `user_id` вместо `username`).
- 🔄 Принято стратегическое решение о переходе на Dependency Injection.
- 🔄 Утвержден план разделения монолитного файла `handlers/transactions.py` на функциональные модули.

## Активные задачи (Refactoring Phase)
1. **Внедрение DI:** Отказ от `Global Service Locator` в пользу явной передачи зависимостей через Middleware или workflow data.
2. **Feature Slicing:** Физическое разделение логики обработки чеков, ручного ввода и истории по разным файлам (`handlers/receipts.py`, `handlers/manual_input.py` и т.д.).
3. **Stabilization:** Улучшение надежности `SyncWorker` (Graceful Shutdown, Retry Policy).

## Следующие шаги (после завершения рефакторинга)
- Внедрение аналитики (построение графиков внутри бота).
- Поддержка полноценной мульти-юзерности (Row Level Security в БД).
- Экспорт отчетов (CSV/Excel) по запросу.