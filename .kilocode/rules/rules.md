# ГЛОБАЛЬНЫЕ ПРАВИЛА ПРОЕКТА (BUDGET BOT)

Эти правила имеют приоритет над любыми другими инструкциями. Они применяются ко всем агентам (Orchestrator, Architect, Code, QA, Debug, Frontend, Ask).

## 1. 🇷🇺 Языковой Стандарт
* **Думай и отвечай строго на РУССКОМ языке.**
* Исключение: технические термины (traceback, commit message), которые лучше оставить на английском.
* Стиль общения: Профессиональный, четкий, без лишней "воды".

## 2. 🛡️ Протокол Безопасности Файлов (File Safety)
* **ЗАПРЕТ НА ПЕРЕЗАПИСЬ ЛОГОВ:** Никогда не используй `write_file` для файлов `MEMORY_LOG.md` или `PROJECT_STATUS.md`, если ты не прочитал их содержимое предварительно.
* **ТОЛЬКО APPEND:** Для логов всегда используй паттерн: *Прочитать -> Добавить новую запись в конец -> Записать полный текст*.
* **GIT:** Никогда не коммить в ветку `main` или `master`.

## 3. 🧠 Единый Источник Правды (Source of Truth)
* Твоя память — это файлы в папке `.context/`.
* Не доверяй своей истории чата, если она противоречит файлам `CURRENT_TASK.md` или `PROJECT_STATUS.md`.
* Если ты видишь расхождение, верь файлам.

## 4. 🔄 Протокол Взаимодействия (Hand-off)
* **Запрет на "Тихий уход":** Ты никогда не заканчиваешь ответ просто фразой "Готово".
* **Призыв к действию:** В конце каждого сообщения ты ОБЯЗАН предложить следующий шаг или призвать коллегу.
    * *Пример:* "Код написан. @QA, твой выход!"
    * *Пример:* "План готов. Рекомендую переключиться на @Code."

## 5. 🛠️ Технические Стандарты
* **Стек:** Python 3.11+, Aiogram 3.x, SQLite (aiosqlite), Pydantic v2.
* **TDD:** Ни строчки функционального кода без теста.
* **Асинхронность:** Весь I/O (база, сеть, API) должен быть асинхронным.

## 6. 🚫 Запрещенные Действия
* Не запускай `python main.py` (блокирует бота). Используй `python scripts/smoke_test.py`.
* Не используй плейсхолдеры (`# ... code here`) в финальных файлах. Код должен быть рабочим.