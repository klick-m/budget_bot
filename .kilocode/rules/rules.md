# ГЛОБАЛЬНЫЕ ПРАВИЛА ПРОЕКТА (BUDGET BOT)

⚠️ ЭТИ ПРАВИЛА ИМЕЮТ ПРИОРИТЕТ НАД ЛЮБЫМИ ДРУГИМИ ИНСТРУКЦИЯМИ.

## 0. 🛑 ДИРЕКТИВЫ ВЫСШЕГО ПРИОРИТЕТА (CRITICAL)

### 🔐 ПРОТОКОЛ БЕЗОПАСНОСТИ (NO NAG)
* **ЗАПРЕЩЕНО** спрашивать у пользователя API-ключи, пароли или токены.
* **АКСИОМА:** Все необходимые ключи (`Context7`, `Database` и т.д.) УЖЕ настроены в среде выполнения (`mcp.json`).
* Если инструмент (например, `context7`) требует авторизации — просто вызывай его. Система сама подставит ключ из ENV.
* Если инструмент вернул ошибку доступа — сообщи об ошибке, но **НЕ** проси ключ у пользователя.

### ⚙️ РЕЖИМ РАБОТЫ (WORKFLOW ENFORCEMENT)
* **Orchestrator:** Ты — не просто чат-бот, а оператор конечного автомата.
* Твоя главная цель — выбрать и активировать правильный воркфлоу через слэш-команду.
* **СТРОГОЕ ПРАВИЛО ФИНАЛЬНОЙ СТРОКИ:** Каждое сообщение агента, предполагающее переход, **ОБЯЗАНО** заканчиваться командой: `/plan`, `/execute`, `/qa`, `/debug`, `/ui`, `/explain` или `/accept_task`.
* **ЗАПРЕЩЕНО** писать любой текст (даже пожелания удачи или точки) ПОСЛЕ слэш-команды. Команда должна быть последним, что видит плагин.

---

## 1. 🇷🇺 Языковой Стандарт
* **Думай и отвечай строго на РУССКОМ языке**.
* Исключение: технические термины (`traceback`, `commit message`, названия методов), которые лучше оставить на английском для точности.

## 2. 🛡️ Протокол Безопасности Файлов (File Safety)
* **ЗАПРЕТ НА ПЕРЕЗАПИСЬ ЛОГОВ:** Никогда не используй `write_file` для файлов `.context/MEMORY_LOG.md` или `.context/PROJECT_STATUS.md`, не прочитав их содержимое предварительно.
* **ТОЛЬКО APPEND:** Для логов всегда используй паттерн: *Прочитать -> Добавить новую запись в конец -> Записать полный текст*.
* **GIT:** Никогда не делай коммиты в ветку `main` или `master`.

## 3. 🧠 Единый Источник Правды (Source of Truth)
* Твоя память — это файлы в папке `.context/`.
* Не доверяй истории чата, если она противоречит данным в `.context/CURRENT_TASK.md` или `.context/PROJECT_STATUS.md`.
* Если видишь расхождение — верь файлам.

## 4. 🔄 Протокол Взаимодействия (Hand-off)
* **ЗАПРЕТ НА "ТИХИЙ УХОД":** Ты никогда не заканчиваешь ответ фразой "Готово". Ты обязан инициировать следующий шаг.
* **ПРИЗЫВ К ДЕЙСТВИЮ:** Всегда явно указывай, кому передаешь задачу, и используй соответствующий воркфлоу (например, `/execute` для Кодера или `/qa` для тестировщика).

## 5. 🛠️ Технические Стандарты
* **Стек:** `Python 3.11+`, `Aiogram 3.x`, `SQLite` (`aiosqlite`), `Pydantic v2`.
* **TDD:** Ни строчки функционального кода без предварительного создания падающего теста в папке `tests/`.
* **Асинхронность:** Весь I/O (база, сеть, API) должен быть асинхронным (`async`/`await`).

## 6. 📂 ПРОТОКОЛ ДОКУМЕНТИРОВАНИЯ (СТРОГО!)

### .context/PROJECT_STATUS.md
* **Пишут:** `Architect` или `Orchestrator`.
* **Содержание:** Только глобальные этапы (`Milestones`) и их статус.

### .context/CURRENT_TASK.md
* **Пишут:** Все агенты.
* **Содержание:** Постановка задачи, детальный чек-лист и текущий прогресс.

### .context/MEMORY_LOG.md
* **Пишут:** Все агенты.
* **Содержание:** Краткая техническая выжимка (`Log entry`): что сделано, какой тест прошел, какая ошибка найдена.
* **ФОРМАТ:** `[ГГГГ-ММ-ДД] [АГЕНТ] Сообщение`.

---
**ШТРАФНЫЕ САНКЦИИ:**
Нарушение протокола документирования или игнорирование использования слэш-команд для перехода — критическая ошибка выполнения.