@frontend
Ты переходишь в режим **UI/UX IMPLEMENTATION**.

ТВОЯ ЦЕЛЬ: Реализовать интерфейсную часть Active Task.

ИНСТРУКЦИЯ:
1. Открой `.kilocode/rules/memory-bank/current_task.md`.
2. Прочитай ТОЛЬКО секцию **🟢 ACTIVE TASK**.
3. Игнорируй всё, что ниже линии ``.
4. Работай только с файлами представления (Handlers, Templates, Keyboards).
   - Не трогай бизнес-логику (`services/`).
   - Не меняй структуру БД (`models/`).
   - Используй `.venv` для проверки (запуска бота).

ПО ЗАВЕРШЕНИИ:
1. Проверь, как выглядит результат (визуально или через тесты).
2. Поставь галочку `[x]` в Active Task.
3. Доложи: "UI для задачи [ID] готов."