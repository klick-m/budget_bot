---
mode: debug
---
# Отладка (Debugging)

**ШАГ 1: АНАЛИЗ ОШИБКИ**
1. Прочитай ошибку в `.context/MEMORY_LOG.md`.
2. Если ошибка в данных, используй инструмент `sqlite` (MCP) для запроса `SELECT * FROM transactions`.

**ШАГ 2: ИСПРАВЛЕНИЕ**
1. Внеси минимальные правки через инструмент `edit_file`.
2. Запусти `python scripts/smoke_test.py` для проверки фикса.

**ШАГ 3: ВОЗВРАТ**
Баг исправлен. Возвращаю на повторный QA.
`/qa`