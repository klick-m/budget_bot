# Приемка Задачи (Acceptance)

**ШАГ 1: АУДИТ**
1. Проверь `MEMORY_LOG.md`:
   - Есть ли `✅ UNIT TESTS PASSED`?
   - Есть ли `✅ SMOKE TEST PASSED`?

**ШАГ 2: БЮРОКРАТИЯ**
* **ЕСЛИ ЧЕГО-ТО НЕТ:**
    * Останови процесс. Верни задачу на доработку (вызови `/workflow qa` или `/workflow execute`).
* **ЕСЛИ ВСЁ ЕСТЬ:**
    1. Обнови `.context/PROJECT_STATUS.md` (поставь `[x]` на текущем пункте).
    2. Добавь запись в `.context/MEMORY_LOG.md`: `🏁 TASK COMPLETED`.
    3. Очисти `.context/CURRENT_TASK.md` (установи статус IDLE).

**ШАГ 3: ФИНАЛ**
* Сообщи пользователю: "Задача полностью закрыта. Система готова к новым поручениям."