# План унификации команд бота

## Текущая ситуация
В боте используется функция `set_commands()` в файле `main.py`, которая регистрирует только 3 команды:
- `/start` - "🔄 Перезапустить бота"
- `/new_transaction` - "💸 Добавить транзакцию вручную"
- `/history` - "📜 Показать историю транзакций"

Однако в обработчиках зарегистрирована дополнительная команда `/test_sheets`, которая отсутствует в меню команд.

## Цель
Создать унифицированную функцию `set_default_commands(dp)` и разместить её в `main.py`, которая будет регистрировать все существующие команды в одном месте.

## Необходимые изменения в main.py

### 1. Заменить функцию `set_commands` на `set_default_commands`
Текущая реализация:
```python
async def set_commands(bot: Bot):
    """Устанавливает команды для меню бота (минимальный набор)."""
    commands = [
        BotCommand(command="start", description="🔄 Перезапустить бота"),
        BotCommand(command="new_transaction", description="💸 Добавить транзакцию вручную"),
        BotCommand(command="history", description="📜 Показать историю транзакций")
    ]
    await bot.set_my_commands(commands)
    logger.info("✅ Меню команд успешно установлено.")
```

Новая реализация:
```python
async def set_default_commands(dp: Dispatcher):
    """Устанавливает полный список команд для меню бота."""
    commands = [
        BotCommand(command="start", description="🔄 Перезапустить бота"),
        BotCommand(command="new_transaction", description="💸 Добавить транзакцию вручную"),
        BotCommand(command="history", description="📜 Показать историю транзакций"),
        BotCommand(command="test_sheets", description="🧪 Проверить соединение с Google Sheets")
    ]
    await dp.bot.set_my_commands(commands)
    logger.info("✅ Полный список команд успешно установлен.")
```

### 2. Обновить вызов функции в основном блоке
Текущая строка 101:
```python
await set_commands(bot) 
```

Должна быть заменена на:
```python
await set_default_commands(dp) 
```

## Результат
После этих изменений все команды, зарегистрированные в боте, будут отображаться в едином меню команд, что устранит путаницу в пользовательском интерфейсе.