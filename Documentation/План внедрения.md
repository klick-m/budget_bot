# План внедрения улучшений Budget Bot

## Обзор

В рамках улучшения Budget Bot необходимо сосредоточиться на надежности ядра системы и сборе данных для будущего обучения. Этот план определяет приоритетную последовательность разработки, которая позволит получить ощутимый результат в точности категоризации и улучшить ежедневное использование бота, прежде чем переходить к более сложным ML-моделям.

## 🚀 Приоритетная последовательность разработки

| Приоритет | Задачи | Цель | Источник |
| :--- | :--- | :--- | :--- |
| **P1** | **Настройка Словаря Ключевых Слов** (с категориями и уровнем уверенности) | **Фундамент надежности.** Самое быстрое и надежное повышение точности категоризации. | Категоризация 3.1 |
| **P1** | **Базовый Препроцессинг** (Нижний регистр, стоп-слова, лемматизация `pymorphy3`) | Сделать текст из чеков чистым и однородным для ML-моделей. | Категоризация 1.1, 2.1 |
| **P2** | **Быстрый ввод транзакции** (формат: "Категория Сумма Комментарий") | Резкое улучшение пользовательского опыта и частоты использования бота. | UI/UX 1 |
| **P2** | **AI-Обратная связь (Кнопки "✅ Правильно" / "❌ Неправильно")** | **Начало сбора чистого датасета.** Главный источник данных для будущего ML. | UI/UX 2 |
| **P3** | **Гибридный Классификатор** | Объединение словаря (P1) и простого TF-IDF. Это даст надежность ручных правил и адаптивность ML. | Категоризация 4.2 |
| **P4** | **Умные Подсказки и Автодополнение** | Использование P1 и P2 для предложения категорий по ходу ввода, ускоряя процесс. | UI/UX 3 |

## Детализация задач

### P1: Настройка Словаря Ключевых Слов

#### Задачи:
- Создать класс `KeywordDictionary` с хранением ключевых слов по категориям с весами
- Реализовать обратный индекс для быстрого поиска
- Добавить поддержку статистики использования и дат обновления
- Внедрить поддержку биграмм (сочетаний слов)

#### Структура хранения:
Вместо хранения всех ключевых слов для категории "Продукты" в одной ячейке Google Sheets, создаем отдельный лист с тремя основными столбцами:

| Столбец (Google Sheets) | Назначение |
|-------------------------|------------|
| КЛЮЧЕВОЕ_СЛОВО | Фактическое слово или фраза (например, "Ашан", "Перекресток", "молоко"). |
| КАТЕГОРИЯ | Категория, к которой это слово относится (например, "Продукты", "Транспорт"). |
| УВЕРЕННОСТЬ / ПРИОРИТЕТ | Числовое значение, показывающее, насколько надежно это слово определяет категорию (например, 1.0 для "Uber", 0.6 для "Кафе"). |

#### Преимущества:
- **Масштабируемость**: Можно добавить сотни ключевых слов, и таблица будет расти вертикально, не "края" ячейку.
- **Управляемость**: Гораздо проще редактировать, удалять или добавлять новые ключевые слова.
- **Готовность к ML**: Эта таблица фактически становится ручным датасетом и словарем признаков (Feature Dictionary) для обучения и оптимизации существующих моделей TF-IDF.

### P1: Базовый Препроцессинг

#### Задачи:
- Внедрение нормализации слов (стемминг/лемматизация) с использованием `pymorphy3` для русского языка
- Внедрить стоп-слова для русского языка и удалять их из текста
- Нормализация регистра (приведение к нижнему регистру)
- Контекстная нормализация (создание словарей синонимов для категорий, нормализация брендов и торговых марок, удаление юридических форм)

#### Пример реализации:
```python
from pymorphy3 import MorphAnalyzer

def preprocess_text(text):
    morph = MorphAnalyzer()
    # Приведение к нижнему регистру
    text = text.lower()
    # Разделение на слова
    words = text.split()
    # Лемматизация
    lemmatized_words = [morph.parse(word)[0].normal_form for word in words]
    return ' '.join(lemmatized_words)
```

### P2: Быстрый ввод транзакции

#### Задачи:
- Внедрить возможность быстрого ввода транзакции в формате "Категория Сумма Комментарий" в одном сообщении (например, "Продукты 256.50 хлеб, молоко")
- Добавить команду /quick для быстрого ввода с автодополнением
- Реализовать сохранение часто используемых шаблонов транзакций

### P2: AI-Обратная связь

#### Задачи:
- Добавить кнопки "✅ Правильно" / "❌ Неправильно" после автоматического определения категории
- При нажатии "Неправильно" - сразу предлагать альтернативные категории, предложенные AI
- Внедрить систему быстрого исправления с возможностью указать правильную категорию и сохранить для обучения модели

### P3: Гибридный Классификатор

#### Задачи:
- Создание интегрированного классификатора, который комбинирует словарь и ML-модель
- Проверка точных совпадений ключевых слов
- Использование TF-IDF для менее очевидных случаев
- Применение бизнес-правил
- Контекстно-зависимая категоризация
- Взвешенное голосование для получения итогового результата

### P4: Умные Подсказки и Автодополнение

#### Задачи:
- При вводе комментария - предлагать автодополнение на основе исторических данных и AI-анализа
- При вводе суммы - показывать средние значения по этой категории за последний период
- Для новых пользователей - предложить часто используемые категории на основе AI-анализа поведения других пользователей

## Рекомендации по фокусу

### Главный совет (фокус на LLM)

Не тратьте ресурсы на дорогие внешние LLM-сервисы (Использование LLM для анализа чеков, Мультимодальные модели), пока гибридная модель (P3), работающая на собранном вами чистом датасете (P2), не перестанет удовлетворять требованиям по точности. Сначала максимизируйте эффективность имеющихся, более дешевых инструментов.

## Заключение

Реализация этого плана позволит значительно повысить точность и надежность системы категоризации транзакций в Budget Bot. Все улучшения могут быть реализованы с использованием традиционных методов программирования и не требуют внешних AI-сервисов, что делает систему более надежной и экономически эффективной.